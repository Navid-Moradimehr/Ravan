apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "data-stream.fullname" . }}-env
  namespace: {{ include "data-stream.namespace" . }}
  labels:
    app: {{ include "data-stream.name" . }}
data:
{{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
