{
  "name": "ravan-operator-shell",
  "private": true,
  "version": "1.0.0-beta.1",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:windows": "tauri build --bundles nsis,msi --ci",
    "build:macos": "tauri build --bundles dmg --ci"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
