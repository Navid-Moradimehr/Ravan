# Training Dataset Benchmark

The benchmark measures local compilation of exported observations into the
portable training bundle. It does not measure Iceberg/S3 throughput or model
training. Record the host, record count, iterations, and output throughput for
comparisons across releases.
