# Updates And Release Operations

## Current behavior

The platform has an opt-in, read-only release check. It can query a JSON
manifest hosted with a GitHub release and report whether a newer version is
available. The API route is `GET /api/v1/system/update-status`, the same-origin
dashboard proxy is `GET /api/update-status`, and the CLI command is:

```text
datastreamctl update check --manifest-url https://example.invalid/release.json
```

The dashboard displays a normal in-app toast when a newer version is found.
The check is disabled unless both of these deployment settings are supplied:

```text
DATASTREAM_UPDATE_CHECK_ENABLED=true
DATASTREAM_UPDATE_MANIFEST_URL=https://github.com/OWNER/REPO/releases/latest/download/release-manifest.json
```

No telemetry, credentials, Kafka data, or historian data is sent. The request
contains only an HTTP `Accept: application/json` header. Air-gapped sites
should leave the setting disabled and distribute release manifests through
their existing change-control process.

## Release manifest

The manifest is small and can be generated by a release pipeline:

```json
{
  "version": "0.4.0",
  "release_url": "https://github.com/OWNER/REPO/releases/tag/v0.4.0",
  "notes_url": "https://github.com/OWNER/REPO/releases/tag/v0.4.0",
  "artifact_url": "https://github.com/OWNER/REPO/releases/download/v0.4.0/package.tar.gz",
  "sha256": "..."
}
```

The current checker validates the version and exposes the metadata. It does
not download `artifact_url`, execute a package, mutate Compose, or migrate a
database.

## Why installation is not automatic yet

An industrial update is a controlled change, not a browser download. A safe
future updater must stage the artifact, verify a publisher signature and
checksum, snapshot configuration and metadata, stop or drain ingestion,
apply compatible migrations, restart the selected services, run health and
smoke checks, and roll back if the checks fail. It must also support an
operator approval policy and an offline/manual path. Implementing only the
download half would create a partial updater and could leave a plant with a
mixed application/database version.

The installer and update agent are intentionally postponed. Until they exist,
operators should use the documented stop, backup, replace, migrate, verify,
and rollback sequence in `docs/self-host-install-guide.md`.

## GitHub release recommendations

1. Publish immutable semantic-version tags such as `v0.4.0`.
2. Attach checksums, a software bill of materials, release notes, and the
   matching manifest to each release.
3. Keep the repository and container image versions aligned.
4. Test the release gate, backup/restore drill, Compose configuration, and
   representative mock soak before publishing.
5. Never make the update check a startup dependency. A GitHub outage must not
   stop ingestion, processing, or historian writes.
6. For plant deployments, mirror approved artifacts to the company's internal
   registry and point `DATASTREAM_UPDATE_MANIFEST_URL` there.
